#разбить line не только по пробелам, но и по многим другим знакам. Кроме того понимает и последовательность знаков,
например "пробел-запятая", "точка-перенос строки".
Единственный момент, оставляет в конце line пустой символ ''. Можно потом удалить.

import re
a = re.split("[., \-!?:;\n]+", line)
